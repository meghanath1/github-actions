name: Maual workflow

on:
  workflow_dispatch:
    
jobs:
  explore-actions:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: echo "Maual Hello, world! The runner is ready."
